# Markdown

`MarkdownProcessor` remplace les références locales (`cover.webp`, `image01.webp`, etc.) par leur chemin public `/uploads/<module>/<nom-unique>.webp`. Le contenu réécrit est celui enregistré en base.
