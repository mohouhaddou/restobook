'use strict';
function safeUrl(value){let u;try{u=new URL(String(value||'').trim())}catch{throw Object.assign(new Error('Invalid media URL'),{status:422})}if(u.protocol!=='https:')throw Object.assign(new Error('Only HTTPS media URLs are accepted'),{status:422});u.username='';u.password='';return u}
function provider(name,hosts,idFromUrl,type='video'){return{name,extractId(value){const u=safeUrl(value);if(hosts.length&&!hosts.some(h=>u.hostname===h||u.hostname.endsWith(`.${h}`)))throw Object.assign(new Error(`Invalid ${name} URL`),{status:422});return idFromUrl(u)||u.href},async metadata(value){const u=safeUrl(value),external_id=this.extractId(value);return{provider:name,media_type:type,external_id,url:u.href,title:`${name} asset ${external_id}`,thumbnail:null,duration:null,description:null}}}}
module.exports={safeUrl,providers:[
provider('vimeo',['vimeo.com'],u=>u.pathname.match(/\/(\d+)/)?.[1]),provider('dailymotion',['dailymotion.com','dai.ly'],u=>u.searchParams.get('video')||u.pathname.split('/').filter(Boolean).pop()),provider('google_drive',['drive.google.com'],u=>u.pathname.match(/\/d\/([^/]+)/)?.[1]||u.searchParams.get('id'),'downloadable'),provider('bunnycdn',[],u=>u.pathname.split('/').filter(Boolean).pop()),provider('cloudflare_stream',['cloudflarestream.com','videodelivery.net'],u=>u.pathname.split('/').filter(Boolean)[0]),provider('local',[],u=>u.pathname,'downloadable')],provider};
