import type{AIProvider}from"./AIProvider";export class ProviderRegistry{private readonly x=new Map<string,AIProvider>();register(p:AIProvider){if(this.x.has(p.id))throw new Error("Provider dupliqué");this.x.set(p.id,p);}get(id:string){const p=this.x.get(id);if(!p)throw new Error(`Provider absent : ${id}`);return p;}list(){return[...this.x.values()];}unregister(id:string){return this.x.delete(id);}}
