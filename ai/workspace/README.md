# Workspace IA

Espace de fichiers uniforme du système IA. `WorkspaceManager.createWorkspace()`
crée et vérifie les répertoires `incoming`, `processing`, `published`, `failed`,
`archive`, `temp` et `logs`. Aucun traitement n'est démarré automatiquement.
