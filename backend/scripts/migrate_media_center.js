#!/usr/bin/env node
'use strict';
require('dotenv').config({path:require('path').join(__dirname,'../.env')});
const {Sequelize}=require('sequelize');
const seq=new Sequelize(process.env.DB_NAME,process.env.DB_USER,process.env.DB_PASS,{host:process.env.DB_HOST||'127.0.0.1',port:Number(process.env.DB_PORT||3306),dialect:'mysql',logging:false});
async function run(){await seq.authenticate();await seq.query(`CREATE TABLE IF NOT EXISTS media (id INT UNSIGNED NOT NULL AUTO_INCREMENT,uuid CHAR(36) NOT NULL,module ENUM('kids','discover','sports','play') NOT NULL,entity_type VARCHAR(64) NOT NULL,entity_id INT UNSIGNED NOT NULL,media_type ENUM('youtube','audio','pdf','image','coloring','downloadable') NOT NULL,provider VARCHAR(40) NOT NULL,title VARCHAR(191) NOT NULL,description TEXT NULL,url VARCHAR(1000) NOT NULL,external_id VARCHAR(191) NULL,thumbnail VARCHAR(1000) NULL,duration VARCHAR(32) NULL,language VARCHAR(10) NOT NULL DEFAULT 'en',sort_order INT NOT NULL DEFAULT 0,featured TINYINT(1) NOT NULL DEFAULT 0,visible TINYINT(1) NOT NULL DEFAULT 1,created_by INT NULL,created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,PRIMARY KEY(id),UNIQUE KEY uq_media_uuid(uuid),UNIQUE KEY uq_media_entity_provider_external(module,entity_type,entity_id,provider,external_id),KEY idx_media_entity_type(entity_type),KEY idx_media_entity_id(entity_id),KEY idx_media_module(module),KEY idx_media_media_type(media_type),KEY idx_media_public_listing(module,entity_type,entity_id,visible,sort_order)) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci`);console.log('Media Center migration complete');await seq.close()}
run().catch(e=>{console.error(e);process.exit(1)});
