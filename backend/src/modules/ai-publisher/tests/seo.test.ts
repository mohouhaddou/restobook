import assert from"node:assert/strict";import test from"node:test";import{SeoImporter}from"../SeoImporter";test("SeoImporter produit les limites et canonical",()=>{const seo=new SeoImporter().generate("T".repeat(80),"D".repeat(200),"discover","titre");assert.equal(seo.title.length,60);assert.equal(seo.description.length,160);assert.equal(seo.canonical,"https://ifilino.com/discover/titre");});
