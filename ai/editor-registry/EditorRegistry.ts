import type{EditorManifest}from"./EditorManifest";export class EditorRegistry{private readonly x=new Map<string,EditorManifest>();register(e:EditorManifest){if(this.x.has(e.id))throw new Error("Éditeur dupliqué");this.x.set(e.id,e);}get(id:string){const e=this.x.get(id);if(!e)throw new Error("Éditeur absent");return e;}list(){return[...this.x.values()];}unregister(id:string){return this.x.delete(id);}}
