import type{ConfigurablePolicy,PolicyResult}from"./ValidationPolicy";export class PublishingPolicy implements ConfigurablePolicy<{status:string}>{evaluate(v:{status:string},c:Readonly<Record<string,unknown>>):PolicyResult{const allowed=(c.allowedStatuses as readonly string[]|undefined)??["approved"];const valid=allowed.includes(v.status);return{valid,errors:valid?[]:["Statut refusé"],warnings:[]};}}
